// `@vueuse/core` is the ninth specifier. The Compose recipe imports it.
// The version tracks frappe-ui's own dependency, never a separately chosen
// one, so a Prototype and the library share one copy of the composables.
export * from '@vueuse/core'
